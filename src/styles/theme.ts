import { extendTheme } from "@chakra-ui/react";

const theme = extendTheme({
  colors: {
  },
  fonts: {
    body: "Montserrat",
    heading: "Montserrat",
  },
  components: {
   
  },
});

export default theme;
